#ifndef COMMON_H
#define COMMON_H

#include <string>
#include <iostream>
#include <vector>
#include <memory>

// We will define our AST nodes here later, but for now we need a place 
// to declare our token types if we were doing it manually. 
// Since Bison generates them, we'll include the bison header in the lexer.

#endif // COMMON_H
